version: '3.8'
services:
  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: SifraZaBazu!
      POSTGRES_DB: RestaurantDb
    ports:
      - "5432:5432"
    restart: always